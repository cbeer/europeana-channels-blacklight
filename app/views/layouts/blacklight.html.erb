<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">

    <!-- Mobile viewport optimization h5bp.com/ad -->
    <meta name="HandheldFriendly" content="True">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">

    <!-- Mobile IE allows us to activate ClearType technology for smoothing fonts for easy reading -->
    <!--[if IEMobile]>
      <meta http-equiv="cleartype" content="on">
    <![endif]-->

    <title><%= render_page_title %></title>
    <%= opensearch_description_tag application_name, opensearch_catalog_url(:format => 'xml') %>
    <%= favicon_link_tag 'favicon.ico' %>
    <%= stylesheet_link_tag "application", media: "all" %>
    <style>
      .search-box-show.music {
        background-color: <%= Channels::Application.config.channels[:music][:'theme-color'] %>;
      }

      .search-box-show.fashion {
        background-color: <%= Channels::Application.config.channels[:fashion][:'theme-color'] %>;
      }

      @media all and (min-width: 60em) {
        body.blacklight-channels-index {
          background-image: url( <%= image_path Channels::Application.config.channels[:home][:hero] %> );
        }

        .hero.channel-music {
          height: 500px;
          background-image: url( <%= image_path Channels::Application.config.channels[:music][:hero] %> );
        }

        .hero.channel-fashion {
          height: 500px;
          background-image: url( <%= image_path Channels::Application.config.channels[:fashion][:hero] %> );
        }
      }
    </style>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= content_for(:head) %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

  </head>
  <body class="<%= render_body_class %> <%= 'hero' if !has_search_parameters? %>">
  <%# render :partial => 'shared/header_navbar' %>

  <%= render partial: 'shared/ajax_modal' %>

  <div id="main-container" class="container">
    <%= render :partial=>'/flash_msg', layout: 'shared/flash_messages' %>

    <div class="row">
      <%= yield %>
    </div>
  </div>

  <%= render :partial => 'shared/footer' %>
  </body>
</html>